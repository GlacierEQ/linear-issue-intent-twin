[project]
name = "linear-issue-intent-twin"
version = "1.0.0"
description = "Executable issue acceptance predicates and merge-blocking intent receipts"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
