[project]
name = "linear-issue-intent-twin"
version = "0.0.1"
description = "Scaffold: Issue Intent Twin (independent Linear lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
